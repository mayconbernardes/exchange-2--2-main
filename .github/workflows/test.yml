name: CI - Testes Flask

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Instalar Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest

      - name: 🔍 Debug - Preparar Ambiente e Verificar Importação
        env:
          DATABASE_URL: "sqlite:///:memory:"
          SECRET_KEY: "test-secret-key"
          PYTHONPATH: "."
          MONGO_URI: "mongodb://127.0.0.1:27017/?serverSelectionTimeoutMS=100"
        run: |
          echo "Criando pastas necessárias para o ambiente CI..."
          mkdir -p flask_session static/audio static/img static/doc
          echo "Listando ficheiros no repositório:"
          ls -R
          echo "Tentando importar o app e capturar erros fatais:"
          python -c "import sys; print('DEBUG Path:', sys.path); import app; print('✅ App importado com sucesso!')"

      - name: 🧪 Executar Diagnóstico e Testes
        env:
          SECRET_KEY: "test-secret-key"
          FLASK_ENV: "testing"
          TESTING: "true"
          DATABASE_URL: "sqlite:///:memory:"
          MONGO_URI: "mongodb://127.0.0.1:27017/?serverSelectionTimeoutMS=100"
          MAIL_SERVER: "localhost"
          MAIL_PORT: "8025"
          MAIL_USE_TLS: "false"
          MAIL_USERNAME: "test@example.com"
          MAIL_PASSWORD: "password"
          MAIL_DEFAULT_SENDER: "test@example.com"
          PYTHONPATH: "."
        run: python -m pytest tests/ -v
